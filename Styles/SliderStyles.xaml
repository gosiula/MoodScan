<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <!--Slider style-->
    <Style x:Key="SliderButtonStyle" TargetType="RepeatButton">
        <Setter Property="SnapsToDevicePixels" Value="true"/>
        <Setter Property="OverridesDefaultStyle" Value="true"/>
        <Setter Property="IsTabStop" Value="false"/>
        <Setter Property="Focusable" Value="false"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="RepeatButton">
                    <Border Height="6"
                            Background="{TemplateBinding Background}"
                            BorderThickness="0"
                            CornerRadius="3"/>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="Command" Value="{x:Static Slider.DecreaseLarge}">
                <Setter Property="Background">
                    <Setter.Value>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                            <GradientStop Color="#396ECD" Offset="0"/>
                            <GradientStop Color="#5727A5" Offset="0.5"/>
                            <GradientStop Color="#672EC5" Offset="1"/>
                        </LinearGradientBrush>
                    </Setter.Value>
                </Setter>
            </Trigger>
            <Trigger Property="Command" Value="{x:Static Slider.IncreaseLarge}">
                <Setter Property="Background" Value="Transparent"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="SliderStyle" TargetType="Slider">
        <Setter Property="Background" Value="#5727A5"/>
        <Setter Property="Foreground" Value="#672EC5"/>
        <Setter Property="Height" Value="30"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Slider">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto" MinHeight="{TemplateBinding MinHeight}"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <Border x:Name="TrackBackground"
                                Grid.Row="1"
                                Height="6"
                                Background="#1E1139"
                                BorderBrush="#2D1B4E"
                                BorderThickness="1"
                                CornerRadius="3"
                                VerticalAlignment="Center">
                            <Border.Effect>
                                <DropShadowEffect Color="#672EC5" 
                                                  BlurRadius="8" 
                                                  ShadowDepth="0" 
                                                  Opacity="0.3"/>
                            </Border.Effect>
                        </Border>

                        <Track x:Name="PART_Track" Grid.Row="1">
                            <Track.DecreaseRepeatButton>
                                <RepeatButton Command="Slider.DecreaseLarge" Style="{StaticResource SliderButtonStyle}"/>
                            </Track.DecreaseRepeatButton>
                            <Track.IncreaseRepeatButton>
                                <RepeatButton Command="Slider.IncreaseLarge" Style="{StaticResource SliderButtonStyle}"/>
                            </Track.IncreaseRepeatButton>
                            <Track.Thumb>
                                <Thumb>
                                    <Thumb.Style>
                                        <Style TargetType="Thumb">
                                            <Setter Property="Width" Value="12"/>
                                            <Setter Property="Height" Value="20"/>
                                            <Setter Property="Template">
                                                <Setter.Value>
                                                    <ControlTemplate TargetType="Thumb">
                                                        <Grid>
                                                            <Border Width="16" Height="24"
                                                                    Background="#672EC5"
                                                                    CornerRadius="3"
                                                                    Opacity="0.3"
                                                                    x:Name="GlowRect">
                                                                <Border.Effect>
                                                                    <BlurEffect Radius="10"/>
                                                                </Border.Effect>
                                                            </Border>

                                                            <Border Width="12" Height="20"
                                                                    CornerRadius="2"
                                                                    x:Name="ThumbRect">
                                                                <Border.Background>
                                                                    <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                                                        <GradientStop Color="#396ECD" Offset="0"/>
                                                                        <GradientStop Color="#5727A5" Offset="0.5"/>
                                                                        <GradientStop Color="#672EC5" Offset="1"/>
                                                                    </LinearGradientBrush>
                                                                </Border.Background>
                                                                <Border.Effect>
                                                                    <DropShadowEffect Color="#000000" 
                                                                                      BlurRadius="8" 
                                                                                      ShadowDepth="2" 
                                                                                      Opacity="0.5"/>
                                                                </Border.Effect>
                                                            </Border>
                                                        </Grid>

                                                        <ControlTemplate.Triggers>
                                                            <Trigger Property="IsMouseOver" Value="True">
                                                                <Setter TargetName="ThumbRect" Property="Width" Value="14"/>
                                                                <Setter TargetName="ThumbRect" Property="Height" Value="22"/>
                                                                <Setter TargetName="GlowRect" Property="Opacity" Value="0.5"/>
                                                            </Trigger>
                                                            <Trigger Property="IsDragging" Value="True">
                                                                <Setter TargetName="ThumbRect" Property="Width" Value="16"/>
                                                                <Setter TargetName="ThumbRect" Property="Height" Value="24"/>
                                                                <Setter TargetName="GlowRect" Property="Opacity" Value="0.7"/>
                                                            </Trigger>
                                                        </ControlTemplate.Triggers>
                                                    </ControlTemplate>
                                                </Setter.Value>
                                            </Setter>
                                        </Style>
                                    </Thumb.Style>
                                </Thumb>
                            </Track.Thumb>
                        </Track>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>